<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduation.mapper_api.UserMapper1">

    <!--根据条件查询用户是否存在-->
    <select id="queryName" parameterType="map" resultType="String">
        select userName from tuser where userId = #{userId}
    </select>


</mapper>